<?xml version="1.0" encoding="UTF-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <ListPreference
        android:dialogTitle="Choose a sort type"
        android:entries="@array/sort_names"
        android:entryValues="@array/sort_clauses"
        android:key="sort_books"
        android:summary="Choose the order the list uses"
        android:title="Sort Books" />
    <ListPreference
        android:dialogTitle="Choose a language"
        android:entries="@array/language_name"
        android:entryValues="@array/language_values"
        android:key="language"
        android:summary="Choose a language"
        android:title="Language" />

</PreferenceScreen>